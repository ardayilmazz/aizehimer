# Netlify repoyu kökten açtığı için uygulama klasörü burada belirtilir.
[build]
  base = "Ai-zheimer"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
